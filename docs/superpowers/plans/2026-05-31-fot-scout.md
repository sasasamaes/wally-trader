# Plan — `/fot-scout` implementation

**Fecha:** 2026-05-31 · **Spec:** `../specs/2026-05-31-fot-scout-design.md`

Scanner regime-aware multi-estrategia para fotmarkets ($50→$500, MT5 manual).

## Estado: ✅ COMPLETO

### Pista 1 — Motor (router)
- [x] `.claude/scripts/fot_scout_router.py` — scan determinista, importable + CLI `--json/--show-all/--asset/--experimental-trend/--phase/--capital`. Venv re-exec + sys.path injection (patrón per_asset_backtest).
- [x] `.claude/scripts/fot_strategy_mapping.json` — mapping régimen→estrategia + edge-gates + per_asset_edge.
- [x] Reuse: `wally_core.{regime.compute_adx/label_regime, validate.validate_setup, hunt.score_asset, multifactor.composite_score}`, `per_asset_backtest.{fetch_binance_klines, fetch_yfinance, atr, donchian, rsi}`, `macross.detect_cross`.
- [x] Seam o/h/l/c/v → open/high/low/close resuelto (`_to_wally`).
- [x] Overlay VOLATILE local (`detect_volatile`, ATR spike 2× media 50).

### Pista 2 — Command + Agent
- [x] `system/commands/fot-scout.md` — profile guard + fotmarkets_guard pre-flight + router + dispatch.
- [x] `system/agents/fot-scout-analyst.md` — refine TV quote + cadena validación + GO/NO-GO + log.
- [x] Sync `.opencode/` vía `bash adapters/opencode/install.sh`.

### Pista 3 — Profile config + logging
- [x] `config.md` phase_1.allowed_assets → `[EURUSD, XAUUSD, BTCUSD, ETHUSD]` (override documentado).
- [x] `memory/scout_proposals.md` seed (log separado, no contamina trading_log/guardian).

### Pista 4 — Tests
- [x] `shared/wally_core/tests/test_fot_scout.py` — 17 casos, todos green.

### Docs
- [x] Spec + este plan.
- [x] Sección `/fot-scout` en `CLAUDE.md`.

## Verificación
- `pytest tests/test_fot_scout.py -v` → 17 passed.
- CLI BTCUSD (Binance, real-time) → RANGE_CHOP, MR, NO_SETUP → WAIT honesto. ✅
- CLI XAUUSD (yfinance) → TREND_EXTREMO → STAND_ASIDE. ✅

## Pendiente / futuro
- Validar pip_value reales por activo en MT5 Specification (sizing es aproximado).
- Re-evaluar el edge de tendencia (MA-Cross/Donchian) con más data antes de subirlo de WEAK.
- A $50 muchos activos dan UNTRADEABLE_SIZE a 1% risk — se resuelve al crecer a Fase 2.
