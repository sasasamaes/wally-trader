# Helpers introducidos a partir del Manual del Trader Algorítmico (PDF).
# Instalar con:  bash .claude/scripts/setup_venv.sh
#
# Uso esperado: ningún script REQUIERE estas deps; degradan a "use --json-dir
# externo" si yfinance no está disponible. Solo per_asset_backtest.py para
# forex/indices se beneficia.

yfinance>=0.2.40

# Polymarket macro sentiment integration (spec 2026-05-02)
httpx>=0.27
pytest-mock>=3.12

# Macro events gate — Forex Factory HTML fallback parsing
beautifulsoup4>=4.12
