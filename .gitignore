# macOS
.DS_Store
._*

# Temp data / backtest artifacts
/tmp/
*.json
!package.json
!package-lock.json
!.claude/settings.json
!.claude/**/*.json
!shared/**/*.json
# OpenCode soporte total — config primary (root) + MCP source-of-truth + legacy back-compat
!opencode.json
!system/mcp/servers.json
!.opencode/config.json
# Hermes adapter — generated skills tree (tracked so clone-and-go works)
!.hermes/skills/**/*.json
# Web app — Next.js + FastAPI bootstrap (feat/web-app-bootstrap)
!web/**/*.json
!api/**/*.json
!infra/**/*.json
!workers/**/*.json

# Screenshots (can be large)
tradingview-mcp/screenshots/
**/screenshots/

# Logs
*.log
*.pid
*.out

# Session-specific ephemeral alert scripts
.claude/scripts/alert_*.sh

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment secrets
.env
.env.local
.env.*.local
*credentials*
*.key
*.pem

# MT5 credentials
.claude/.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/

# Node
node_modules/
!tradingview-mcp/node_modules/

# OS
Thumbs.db

# Claude project workdir (if any)
.claude/projects/

# Claude settings locales (overrides personales)
.claude/settings.local.json

# Nested git repos (tracked separately upstream)
claude-tradingview-mcp-trading/

# Discipline & Observability runtime cache (macro events + bitunix log errors)
.claude/cache/

# ML System — cache de data histórica Binance y modelos entrenados
scripts/ml_system/data/
scripts/ml_system/supervised/model/*.pkl
scripts/ml_system/supervised/model/*.json
scripts/ml_system/**/*.parquet

# Git worktrees (isolated dev branches)
.worktrees/
.claude/worktrees/

# Personal trading log — gitignored para open source (contiene capital real + trades específicos)
.claude/profiles/retail/memory/trading_log.md
.claude/memory/trading_log.md
.claude/scripts/.venv/

# Sensitive — wally-trader v2
.claude/profiles/*/memory/tax_*.csv
.claude/profiles/*/memory/why_log.jsonl
.claude/profiles/_audit/
**/notify_errors.log
**/cooldowns.json
.wally/
~/.wally/

# Logs
logs/

# Temporary
**/.DS_Store

# Mobile auth token
**/mobile_token*

# Build artifacts
shared/wally_core/build/
.pytest_cache/
**/.pytest_cache/

# Typo worktree dirs from prior sessions
.claire/
.clone/

# Strategy imports (raw + distilled JSON — local cache)
.claude/strategy_imports/
.claude/cache/yt_subtitles/

# Watcher writes these on a per-minute schedule — too noisy to track
.claude/watcher/status.json
.claude/watcher/dashboard.md
